name: First Workflow
on : workflow_dispatch
jobs :
  my-first-job :
    runs-on : ubuntu-latest
    steps:
      - name: Print Greeting
        run : echo "Hello World"
      - name : Say GoodBye
        run : echo "Good Bye"
      
