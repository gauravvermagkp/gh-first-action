name: First Workflow
on : workflow_dispatch
jobs :
  my-first-job :
    runs-on : ubuntu-latest
    steps:
      - name: Print Greeting
        run : |
          echo "Hello World"
          echo "Hello Again"
      - name : Say GoodBye
        run : echo "Good Bye"
      - name : checkout_code
        uses : actions/checkout@v4
      
